SELECT DEPARTMENT.Name, MAX(EMPLOYEE.SALARY) as MaxSalary, MIN(EMPLOYEE.SALARY) as MinSalary, AVG(EMPLOYEE.SALARY) as AvgSalary FROM `EMPLOYEE` LEFT JOIN DEPARTMENT ON DEPARTMENT.Department_id = EMPLOYEE.DEPARTMENT_ID